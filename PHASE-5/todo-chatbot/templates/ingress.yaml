{{- if or .Values.backend.ingress.enabled .Values.frontend.ingress.enabled }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "todo-chatbot.fullname" . }}
  labels:
    {{- include "todo-chatbot.labels" . | nindent 4 }}
  annotations:
    {{- if .Values.tls.enabled }}
    cert-manager.io/cluster-issuer: {{ .Values.tls.clusterIssuer | default "letsencrypt-prod" | quote }}
    {{- end }}
    {{- with .Values.backend.ingress.annotations }}
    {{- toYaml . | nindent 4 }}
    {{- end }}
spec:
  {{- with .Values.backend.ingress.className }}
  ingressClassName: {{ . }}
  {{- end }}
  {{- if .Values.tls.enabled }}
  tls:
    - hosts:
        {{- if .Values.frontend.ingress.enabled }}
        - {{ .Values.frontend.ingress.host | quote }}
        {{- end }}
        {{- if .Values.backend.ingress.enabled }}
        - {{ .Values.backend.ingress.host | quote }}
        {{- end }}
      secretName: {{ .Values.tls.secretName | default "todo-tls" }}
  {{- end }}
  rules:
    {{- if .Values.frontend.ingress.enabled }}
    - host: {{ .Values.frontend.ingress.host | quote }}
      http:
        paths:
          - path: {{ .Values.frontend.ingress.path }}
            pathType: Prefix
            backend:
              service:
                name: {{ include "todo-chatbot.fullname" . }}-frontend
                port:
                  number: {{ .Values.frontend.service.port }}
    {{- end }}
    {{- if .Values.backend.ingress.enabled }}
    - host: {{ .Values.backend.ingress.host | quote }}
      http:
        paths:
          - path: {{ .Values.backend.ingress.path }}
            pathType: Prefix
            backend:
              service:
                name: {{ include "todo-chatbot.fullname" . }}-backend
                port:
                  number: {{ .Values.backend.service.port }}
    {{- end }}
{{- end }}
